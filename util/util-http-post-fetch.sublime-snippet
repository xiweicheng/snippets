<snippet>
	<content><![CDATA[
this.http.fetch(nsApi.url('${1:}'), {
    method: 'post',
    body: json({
        ${2:}
    })
}).then((resp) => {
    if(resp.ok) {
        toastr.success('${3:TODO...}');
    }
});
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>ut-http-post-fetch</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.js</scope>
</snippet>
